specVersion: "v1beta"
serviceId: "vibio-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "vibio-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
